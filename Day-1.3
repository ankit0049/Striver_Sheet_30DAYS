/*
   We Know In dictonary the word Assemble in lexiographically
   order similary here the rule Applicabel that is 
   the number next genrated is greater then previous one 
   (minimum greater). if the last permutation comes and said 
   find next so return the intial form or in the revrse order .

*/

public static void NextPermutation(int arr[])
{ 
    int ind = -1;
    for(int i=arr.length-1; i>0; i--)
    {
        if(arr[i]>arr[i-1]) 
        {
            ind=i;
        }
    }
     if(ind==-1)
     {
     revrse(arr); // simply revrse this is not implemented here
     }
    else(ind!=-1)
    {
        for(int i=arr.length; i>=ind; i--)
        {
            if(arr[i]>arr[ind])
            {
                swap(i,ind,arr);
                break;
            }
        }
    } 

    reverse(arr,ind+1,arr.length);

}